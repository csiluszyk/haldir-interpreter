fib :: Int -> Int
    n :: Int =
        let fibaux :: Int -> Int -> Int -> Int
            a :: Int
            b :: Int
            n :: Int =
                if eq n 0 then
                    a
                else
                    fibaux b (add a b) (sub n 1)
        in
        fibaux 0 1 n
;

fib 7 ;
